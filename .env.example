# Required in production so contact and AI-check leads are delivered reliably.
# RESEND_API_KEY is a server-only secret. Never expose it through PUBLIC_*.
RESEND_API_KEY=
CONTACT_FROM_EMAIL=
CONTACT_TO_EMAIL=pascal.misoph@gmail.com

# Public GA4 measurement identifier. Google Analytics is loaded only after opt-in.
PUBLIC_GA_MEASUREMENT_ID=
